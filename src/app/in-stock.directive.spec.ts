import { InStockDirective } from './in-stock.directive';

describe('InStockDirective', () => {
  it('should create an instance', () => {
    const directive = new InStockDirective();
    expect(directive).toBeTruthy();
  });
});
